#new_list_modal.modal.fade{:tabindex => "-1", :role => "dialog", :aria => { :labelledby => "new_list_modal_label", :hidden => "true"} }
  .modal-dialog
    .modal-content
      .modal-header
        %h4.modal-title New List

      = simple_form_for List.new, wrapper: :horizontal_form, remote: true, html: { class: 'form-horizontal' } do |f|
        .modal-body
          = f.input :name

        .modal-footer
          = f.button :submit, class: 'btn btn-primary'
          %button.btn.btn-default{ :data => { :dismiss => "modal" } } Close
